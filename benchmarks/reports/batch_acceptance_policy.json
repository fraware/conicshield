{
  "schema_version": "conicshield_batch_acceptance_policy/v1",
  "description": "Vendor-host acceptance thresholds for native compiled batch vs sequential microbatch.",
  "min_batch_size": 4,
  "min_speedup_ratio": 0.98,
  "acceptance_mode": "any_row_meets",
  "device": "cpu",
  "notes": "Vendor CI: performance_benchmark --sweep --batch-sizes 4,8,16. At least one row must show compiled batch within 2% of sequential microbatch (viability). Throughput wins (speedup > 1.05) are reported in batch_solve_report but not required on tiny CPU sweep scenarios."
}
