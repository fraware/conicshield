# Published run `host-realistic-20260525` (host-realistic export evidence)

Canonical **upstream-shaped** export: `benchmarks/external_evidence/offline_graph_export_upstream.json` (not `tests/fixtures/offline_graph_export_minimal.json`).

Re-run ``scripts/run_host_realistic_publish.py`` with ``--no-passthrough`` and ``--include-native-arm`` on a licensed host to upgrade ``projector_mode`` and native arms; refresh ``benchmarks/PUBLISHED_RUN_INDEX.json`` after changes.
