# Benchmark Runs

Each subdirectory under `benchmarks/runs/` is an immutable run bundle.

A run bundle must contain:
- config.json
- summary.json
- benchmark_card.md
- episodes.jsonl
- transition_bank.json
- any required schema and provenance files
