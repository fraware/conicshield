# Test layers

Trust layers are exercised by pytest subtrees and `make` targets (`env-check`, `smoke-check`, `reference-correctness`, parity, governance).

Public verification: `make onboard`, `make verify-v1-lock-quick`. See [`docs/COMMUNITY_LAYER.md`](../docs/COMMUNITY_LAYER.md).
