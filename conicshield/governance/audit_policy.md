# Audit policy (in-tree)

`audit_cli --strict` enforces index integrity, bundle profiles, and governance invariants before merge.

Run locally: `make verify-reference-system` and `make verify-v1-lock-quick`.

Public surface: [`docs/COMMUNITY_LAYER.md`](../../docs/COMMUNITY_LAYER.md).
