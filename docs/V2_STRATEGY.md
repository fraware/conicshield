# v2 strategy (explicit next chapter)

v1 delivered: governed benchmark family, flagship host-realistic release, reference authority CI, native batch API, binding merge attestation (Policy B).

## Recommended direction: Option A — Deepen the same family

**Default v2 chapter** for the next 6–12 months:

| Theme | Work |
|-------|------|
| Repeatable host-realistic ops | [`HOST_REALISTIC_REFRESH_PROCEDURE.md`](HOST_REALISTIC_REFRESH_PROCEDURE.md), scheduled refresh cycles |
| Reference-grade artifacts | [`PUBLISHED_BUNDLE_CATALOG.md`](PUBLISHED_BUNDLE_CATALOG.md), index `catalog`, bundle READMEs |
| Batch as product surface | [`SOLVER_PATHS_AND_BATCHING.md`](SOLVER_PATHS_AND_BATCHING.md), vendor acceptance thresholds |
| Merge law | [`REVIEWER_MERGE_CHECKLIST.md`](REVIEWER_MERGE_CHECKLIST.md) |
| Richer reports | Trust dashboard, batch reports, optional retraining comparisons under frozen family contract |

**Why:** Lowest risk; maximizes credibility of the one family you already publish.

## Option B — Benchmark framework across families

Publish `conicshield-shield-qp-micro-v1` with a real `current_run_id`, then add a second distinct host/family to prove governance generalizes.

**When:** After Option A refresh cadence runs cleanly twice.

## Option C — Differentiable shielding frontier

Promote autograd only per [`DIFFERENTIATION_PUBLIC_STANCE.md`](DIFFERENTIATION_PUBLIC_STANCE.md) promotion criteria.

**When:** Only if product strategy requires learning-loop integration as the next bet.

## Decision record

| Option | Status |
|--------|--------|
| A — Deepen same family | **Active (v2 default)** |
| B — Multi-family platform | Deferred until A operational |
| C — Differentiable product | Deferred by policy |
